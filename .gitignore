node_modules/
dist/

# npm CLI bundle — build artifact of `npm run build:cli`, rebuilt by prepack.
bin/cairn.mjs
# `npm pack` output (scripts/smoke-npm.sh packs to a temp dir, but a manual
# `npm pack` lands here).
*.tgz

# Scratch space for experiments — probe scripts, before/after SVGs, sweep dumps.
# Never source; a `git add -A` swept 39 of these into a merge commit once.
.tmp/

*.log
*.bun-build
bun.lock

.opencode/*
!.opencode/skills

.claude/*
!.claude/skills
.vercel
.env*

graphify-out/

# Added by code-review-graph
.code-review-graph/
