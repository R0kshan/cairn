node_modules/
dist/

# npm CLI bundle — build artifact of `npm run build:cli`, rebuilt by prepack.
bin/cairn.mjs
# `npm pack` output (scripts/smoke-npm.sh packs to a temp dir, but a manual
# `npm pack` lands here).
*.tgz

*.log
*.bun-build
bun.lock

.opencode/*
!.opencode/skills

.claude/*
!.claude/skills
.vercel
.env*

# Added by code-review-graph
.code-review-graph/
